"""SimCLR-style encoders + controls (backbone f, projector g, NT-Xent).

Submodules: backbone, projector, simclr, augmentations, train_simclr,
random_encoder (random-encoder control), supervised (supervised reference).
"""
