"""Source package for the probe-capacity / contrastive-invariance study.

Run entry points from the repo root as modules, e.g.::

    python -m src.data.shapes3d --download
    python -m src.encoders.train_simclr --config configs/run/pilot_mps.yaml
    python -m src.eval.quality_gate --config configs/run/pilot_mps.yaml ...
"""
