% ============================================================
% Probe Capacity & Contrastive Invariance -- paper skeleton
% Compile with pdfLaTeX (or upload as-is to Overleaf)
% STATUS: outline only -- Phase 1 (encoder pipeline) not yet complete.
% Results-dependent sections are placeholders; fill in once the
% sweep (Sec 7 of ResearchOverview.md) finishes.
% ============================================================

\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{float}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage[round]{natbib}
\bibliographystyle{plainnat}

\newcommand{\R}{R}          % recoverability
\newcommand{\Sel}{S}        % probe selectivity
\newcommand{\G}{G}          % encoder gain
\newcommand{\epsG}{\varepsilon_G}

% TITLE RETIRED 2026-08-25 (D033). "When ``Invariant'' Means ``Linearly Inaccessible''"
% promises the verdict-stability flip count, which A14(h) fixed as structurally unfirable
% before unblinding and which came back at 1 with CI95 [0,1]. Do not ship it.
% Candidates, from what was actually measured (positioning-2026-08-25.md \S1):
%   - Two Regimes of Linear Inaccessibility in Contrastive Representations
%   - Do Augmentations Move the Factors They Name? A Factorial-Grid Audit of SSL Invariance
%   - What a Linear Probe Cannot Tell Apart: Capacity Closure in Self-Supervised Invariance
% Pick after notebook 08.
\title{Do Augmentations Move the Factors They Name? \\
A Factorial-Grid Audit of Self-Supervised Invariance}
% PROVISIONAL -- see the note above.

\author{Author Name \\ Affiliation \\ \texttt{email@example.com}}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
% TODO (write last, after results): 150--250 words covering the
% measurement confound, the probe-capacity-ladder method, the
% headline flip-count / G-map result, and the takeaway for
% augmentation-choice practice.
Placeholder abstract.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}
% THESIS REWRITTEN 2026-08-25. Source of truth:
% lab-notebook/positioning-2026-08-25.md \S1. ResearchOverview.md \S1 is superseded.
%
% Thesis: SSL invariance is measured by asking whether a probe can recover a
% generative factor, then reported as a property of the representation. Two
% assumptions sit underneath and neither is routinely checked: that the
% augmentation manipulates the factor it is named for, and that a
% fixed-capacity probe's failure means the factor is not there. On a complete
% factorial grid of independently sampled factors both are directly testable.
% Both fail in ways that change the verdict.
%
% CLAIM LADDER (positioning \S2) -- present in this order:
%   1. LEAD. Augmentation-to-factor correspondence audit. The only axis that
%      came back unhit. A5 SO(2) vs SO(3); A7 whole-scene affine zoom vs
%      object-only scale; A7(c) hue shortcut (the hue triple identifies an
%      image 1 in 1000). Cite von Kugelgen \S6 as the direct warrant.
%   2. RESULT. The closure-fraction dissociation. Among readouts a linear probe
%      reports identically, capacity separates closure from widening. Colour arm
%      and control arm have NON-OVERLAPPING kappa intervals of opposite sign on
%      the same three hue factors.
%   3. CONFIRMATION, PENDING. The probe-free displacement certificate
%      (notebook 08). Do NOT promise it in the introduction until it has run.
%   4. PRECONDITION, NOT A CONTRIBUTION. The random-encoder headroom ceiling.
%      Concede to Cao et al. NeurIPS 2022, Schott et al. ICLR 2022, Maziarka
%      Table 5(e), Saxe et al. ICML 2011 BEFORE using it. No abstract sentence
%      may rest on it (positioning \S3).
%
% NARRATIVE INVERSION -- state it plainly, it is the more interesting claim:
% H1 is confirmed mostly on the CONTROL arm. "More capacity recovers what a
% linear probe missed" is true of the un-augmented baseline and false of the
% augmented one.
%
% BANNED FROM THIS SECTION: "first", "novel", the flip count as a headline, and
% any reading of kappa < 0 as "destroyed" before notebook 08 has run.

\section{Related Work}
\label{sec:related}
% Structure per ResearchOverview.md \S4 (gap) -- do NOT draft prose
% before entering the writing phase; see CLAUDE.md "Writing-phase
% reminders" for the additional citations to weave in at that point
% (Lee \& Kondor 2026; Chaudhry 2026; Yu et al. 2026) and the
% scoping directive (novelty is the controlled invariance-measurement
% map, not the general "nonlinear probe recovers more" claim).
\subsection{Probing methodology}
% Hewitt \& Liang control tasks; Pimentel et al. counterpoint (powerful probes).
\subsection{What contrastive learning encodes}
% von K\"ugelgen et al.; Tian et al.; HaoChen et al.; Balestriero \& LeCun.
\subsection{Encoder vs.\ projector}
% Cosentino et al. 2022 -- basis of H4; Chaudhry 2026 (writing-phase add).
\subsection{Probe-free and interventional invariance measurement}
% NON-NEGOTIABLE CITATIONS, verified first-hand (concession-list.md \S2.3, \S3):
% Ericsson, Gouk \& Hospedales BMVC 2022 (2111.11398) -- already measure
%   invariance as a whitened Mahalanobis displacement on a frozen backbone over
%   18 transforms. That is our m_total. Concede up front, in these terms: they
%   measure displacement on AUGMENTED pairs; the complete factorial grid lets the
%   same quantity be computed on exact GENERATIVE COUNTERFACTUALS, which is what
%   makes a null, a calibration and a decomposition available at all.
% Wang \& Isola ICML 2020 -- alignment is the unwhitened m_total.
% Suter et al. IRS ICML 2019; Goodfellow et al. NIPS 2009.
% SEIS (2602.04054) -- OWNS the loss-versus-re-encoding framing sentence, and
%   reports a random-network baseline. Concede early and explicitly.
% EditCLEVR (2607.22705) -- closest paper to the whole design.
% Draft paragraph ready to drop in: positioning-2026-08-25.md \S6.3.

\subsection{Positioning}
% Source of truth: lab-notebook/concession-list.md (dated, first-hand verified).
% It supersedes newplan.md \S3.1 and literature-gaps-2026-08-20.md.
% Delete every "first" and "novel". TMLR does not score novelty but does reject
% for claiming it falsely. State the surviving deltas TOGETHER, since none works
% alone: independently sampled factors (kills the latent-dependency account
% available to von Kugelgen), the random-encoder control Rashtchian \S B.1 names
% as their missing control, exact generative counterfactuals over a complete
% grid, and a calibrated absolute threshold with a declared unadjudicated band.
% Say "approximately coordinate-stable", never "stable under affine
% reparameterization": A15 measured 17.2\% mean relative deviation in m_total at
% var_fraction 0.95.

% ============================================================
\section{Problem Setup and Definitions}
\label{sec:defs}
% Copy verbatim from preregistration/prereg.md \S\S1--5 (LOCKED 2026-06-28).
% Do not restate informally -- these are frozen operational definitions.

\subsection{Generative factors and ground truth}
% Shapes3D (floor hue, wall hue, object hue, scale, orientation -- continuous;
% shape -- categorical) primary; dSprites (shape, scale, orientation, x, y) secondary.

\subsection{Recoverability, selectivity, and encoder gain}
% R(F,c): probe-test performance at capacity c, normalized [0,1]
%   categorical: (acc - chance)/(1 - chance); continuous: R^2 (unclipped).
% S(F,c) = R(real labels) - R(random labels)      [Hewitt-Liang control task]
% G(F,c) = R(trained encoder) - R(random encoder) [headline metric]
% Capacity gap: Delta_G(F) = G(F, top rung) - G(F, linear).
% Commensurability note (FIX 1): normalized-accuracy and R^2 never pooled
% or rank-compared across factor types.

\subsection{$\epsG$-invariance (dual-gate definition)}
% G(F,c) <= eps_G            => invariant (report G<0 sub-case separately)
% G(F,c) > eps_G and S > 0   => genuine recovery
% G(F,c) > eps_G and S <= 0  => inconclusive / probe-driven (dead zone)
% eps_G primary: data-derived per-factor (95% CI bound, random-vs-random
% encoder null); eps_G = 0.05 normalized is a sensitivity analysis only.
%
% SEMANTICS DISCIPLINE (enforce everywhere in prose/tables): eps_G is a
% DETECTION threshold — "G <= eps_G" reads as "no detectable encoder gain
% over the random-encoder null", NEVER as "the factor is absent from the
% representation". Absolute R levels are co-reported next to every verdict
% (prereg A1c), and readouts whose random floor is near ceiling carry a
% null-saturated flag with a saturation-excluded flip count co-reported
% (prereg A4a). Any sentence quoting the headline flip count must quote the
% primary and saturation-excluded variants together.

% ============================================================
\section{Method}
\label{sec:method}

\subsection{Encoders}
% SimCLR-style: backbone f (small ResNet / compact CNN), projector g,
% NT-Xent/InfoNCE, temperature tau. Probe frozen f; report projector
% results separately (Cosentino-style axis, H4).

\subsection{Augmentation conditions}
% Realized grid (prereg Amendment A1): the STRONG cross-section only —
% Color / Position / Orientation / Scale / Control-aug at strong strength,
% >=12 seeds per cell; the weak axis is deferred to a follow-up study.
% Arm -> perturbed-factor map (prereg A4d): each augmentation perturbs a
% SET of factors; every "the augmentation targeting factor F" claim is read
% against this table.
\begin{table}[H]
\centering
\caption{Augmentation conditions: targeted factors and the full set of
factors each transform perturbs (prereg A4d).}
\label{tab:conditions}
\begin{tabular}{llll}
\toprule
Condition & Augmentation & Targeted factor(s) & Also perturbs \\
\midrule
Color       & hue/color jitter          & floor/wall/object hue & --- \\
Position    & random crop / translation & x, y position         & scale, aspect \\
Orientation & rotation                  & orientation           & apparent scale \\
Scale       & random resized crop       & scale                 & position \\
Control-aug & minimal (crop-only)       & --- (baseline)        & position, scale \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Probe-capacity ladder}
% (1) linear -- (2) MLP 1-hidden small -- (3) MLP 1-hidden large --
% (4) MLP 2-hidden. Monotone capacity; capacity reported as an explicit
% measure (param count / effective DOF) per rung (deferred to probe-build,
% engaging Lee \& Kondor 2026 at writing time).
% Capacity-axis validity (prereg A6): "G rises with capacity" (H1) is only
% well-posed if the ladder is a MONOTONE capacity axis, so the frozen measure
% (per-rung trainable-param count) is CHECKED, not asserted (analyze_cell ->
% capacity_axis, per-factor non-decreasing verdict); argue the four rungs'
% approximate function-class nesting against Lee & Kondor's principled probe
% hierarchy here; effective DOF co-reported as a probe-build diagnostic.

\subsection{Controls}
% Random-LABEL control (defines S); random-ENCODER control, matched
% architecture, untrained (defines G); supervised-reference encoder
% (diagnostic ceiling only, not in any decision rule); encoder-quality
% gate (downstream linear-probe accuracy) before any invariance claim.

\subsection{Experimental design}
% Realized factorial sweep (A1): factors (5-6) x conditions (4+control) x
% strong strength x probe capacities (4) x seeds (12). Seed controls both
% encoder and probe init. Probe-build pins (prereg A4c): per-dimension
% standardization on probe-train stats; identical training protocol at every
% rung (Adam lr 1e-3, batch 4096, 100 epochs, weight-decay grid tuned on
% val — tuning budget rung-uniform by construction); splits 40k/10k/30k,
% split_seed fixed study-wide; probe-test-only reporting.
% Diagnostics outside all decision rules: raw-pixel reference ladder (A4b),
% null-headroom flags (A4a), supervised-reference ceiling.

% ============================================================
\section{Hypotheses}
\label{sec:hyp}
% Reproduce H1-H4 verbatim from prereg.md \S6 (LOCKED) with their
% confirm/refute rules -- write this section from the frozen file,
% not paraphrased.
\begin{itemize}
  \item \textbf{H1 (capacity dependence).}
  \item \textbf{H2 (heterogeneity).}
  \item \textbf{H3 (genuine invariance via control).}
  % prereg A6 null-saturation GATE: H3 confirms iff >=1 NON-null-saturated factor
  % stays G <= eps_G at every rung. A null-saturated readout (random floor near
  % ceiling) satisfies G <= eps_G trivially for want of headroom, so it is EXCLUDED
  % from the genuine-invariance verdict (A3 vacuity logic applied to saturation) and
  % retained as a tagged diagnostic (invariant_but_null_saturated). State this gate
  % where H3 is defined -- it is what prevents H3 confirming off a measurement floor.
  \item \textbf{H4 (encoder-vs-projector localization).}
\end{itemize}

% ============================================================
\section{Statistical Analysis Plan}
\label{sec:stats}
% Bootstrap CIs on G, S, Delta_G (paired seed resampling shared across
% gates); effect sizes with CIs; Wilcoxon signed-rank for paired
% comparisons; Holm correction across the factor x condition family;
% report per-cell, never collapse to a single average (prereg.md \S7).
% Maximal-Holm-family disclosure (prereg A6): report the pre-registered maximal
% strong-slice family (DESIGNED_STRONG_CELLS), the realized cells, and every
% pre-verdict exclusion with its reason (holm_family) -- so the realized-family
% correction is seen to shrink BY DESIGN (shape-gate / construct / identifiability
% removals, all pre-verdict), not as post-hoc power gaming. Wilcoxon power at
% n=12 after Holm: nulls reported as non-detection (D025), never as homogeneity.

% ============================================================
\section{Results}
\label{sec:results}
% PLACEHOLDER -- do not draft until the core sweep (ResearchOverview.md
% \S15, Months 3-4) completes and the encoder-quality gate passes.

\subsection{Encoder-quality gate}
\subsection{Per-factor recoverability-vs-capacity curves}
% Co-plot absolute levels: R(trained), R(random floor), R(projector) (A1c)
% and the raw-pixel reference ladder (A4b) so floor height is always visible.
\subsection{Encoder gain G and the capacity gap $\Delta_G$}
\subsection{Verdict-stability flip count}
% Quote primary AND saturation-excluded counts together (A4a), each with the
% A1c seed-bootstrap interval, for both eps_G variants; list null-saturated
% readouts explicitly.
% prereg A6: the count QUOTED IN CLAIMS is the null-saturation-excluded variant
% (headline_for_claims); a flip on a saturated readout cannot distinguish absence
% from lack of floor headroom. The all-factor primary and fixed-0.05 remain
% co-reported; never quote the primary alone as the headline.
\subsection{Encoder vs.\ projector (H4)}
% Descriptive sign only (A1): single-strength grid; no confirm/refute claim.

% ============================================================
\section{Discussion}
\label{sec:discussion}
\subsection{Interpretation}
\subsection{Implications for augmentation choice}
\subsection{Scope of claims}
% CLAIMS THE PAPER MAKES (each scoped to the realized grid): statements of
% the form "factor F is/is not recoverable from the frozen encoder at
% capacity c above the random-encoder null, under condition X at strong
% strength, on dataset D" — nothing stronger.
% CLAIMS THE PAPER MUST NOT MAKE:
%   - no generalization to other SSL objectives (SimCLR only), other
%     augmentation strengths (strong only, A1), real data (synthetic only;
%     the bounded real-data check, if unrun, is omitted, not approximated),
%     larger scales (small backbone, 64x64), or unstudied factors;
%   - no reading of "G <= eps_G" as "information absent" (see Sec. 3
%     semantics discipline; null-saturated readouts flagged, A4a);
%   - no attribution of the headline contrast to factor identity alone:
%     object hue lives on Shapes3D, position on dSprites, so the contrast
%     is also a dataset contrast (A4d); H2's confirmatory within-type pairs
%     are within-cell and unaffected;
%   - no theoretical capacity claim for the ladder: it is four probe
%     families downstream users actually deploy, reported by param count;
%     engage Lee & Kondor 2026 here (writing-phase note);
%   - H2/H4 nulls are reported as "not detected at n=12 after Holm",
%     never as evidence of homogeneity / absence of localization.
\subsection{Limitations}
% Threats to validity per ResearchOverview.md \S11: probe overfitting
% (mitigated by control + held-out test), synthetic-vs-real scope,
% projector confound, encoder undertraining, capacity/sample-size
% confound (probe-train size fixed).
% Add: null-headroom compression of G where the random floor nears ceiling
% (A4a diagnostic + saturation-excluded flip count); single-strength grid
% (A1) => H4 descriptive; small-scale SimCLR (batch size bounds the NT-Xent
% negative pool) => report batch/tau/epochs per cell; Wilcoxon power at
% n=12 after Holm (pre-registered caveat + D025 calibration).

\section{Conclusion}
\label{sec:conclusion}

\section*{AI Disclosure}
% Per preregistration/prereg.md \S7: literature search and drafting
% assistance used AI tools; recorded for transparency.

\section*{Acknowledgments}

\bibliography{references}

\end{document}
